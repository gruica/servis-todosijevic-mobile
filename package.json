{
  "name": "servis-todosijevic-standalone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed - using standalone HTML' && exit 0"
  },
  "devDependencies": {
    "@capacitor/android": "6.0.0",
    "@capacitor/cli": "6.0.0",
    "@capacitor/core": "6.0.0",
    "typescript": "^5.9.2"
  }
}
